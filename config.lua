-- =============================================================================
-- FIREDEPT UI LIBRARY - CONFIGURATION
-- =============================================================================

UIConfig = {
    -- ==========================================================================
    -- GENERAL SETTINGS
    -- ==========================================================================

    DefaultTheme = 'default', -- default, fire, dark

    Debug = false,            -- Enable debug overlay

    -- ==========================================================================
    -- RENDERER SETTINGS
    -- ==========================================================================

    Renderer = {
        UpdateInterval = 0, -- 0 = Every frame, 100 = Every 100ms
    },

    -- ==========================================================================
    -- NOTIFICATION SETTINGS
    -- ==========================================================================

    Notifications = {
        Position = 'top-right', -- top-left, top-center, top-right, etc.
        DefaultDuration = 3000, -- Milliseconds
        MaxVisible = 5,
        PlaySound = true
    },

    -- ==========================================================================
    -- PANEL SETTINGS
    -- ==========================================================================

    Panels = {
        DefaultWidth = 0.20, -- 20% of screen width
        AutoHide = true,
        HideDelay = 500      -- Milliseconds
    },

    -- ==========================================================================
    -- DIALOG SETTINGS
    -- ==========================================================================

    Dialogs = {
        -- Stub for future
    },

    -- ==========================================================================
    -- MENU SETTINGS
    -- ==========================================================================

    Menus = {
        -- Stub for future
    }
}

print("^2[FD-UI Config] Loaded^0")
