-- =============================================================================
-- COMPONENT STUBS - For Future Expansion
-- =============================================================================

-- =============================================================================
-- PROGRESS BAR COMPONENT (Standalone)
-- =============================================================================

UIProgressBar = {}

function UIProgressBar.new(config)
    -- Stub: Create standalone progress bar
    -- Can be used outside of panels
    return {}
end

print("^2[FD-UI ProgressBar] Loaded (Stub)^0")

-- =============================================================================
-- TEXT COMPONENT (Standalone)
-- =============================================================================

UIText = {}

function UIText.new(config)
    -- Stub: Create standalone text element
    return {}
end

print("^2[FD-UI Text] Loaded (Stub)^0")

-- =============================================================================
-- BUTTON COMPONENT
-- =============================================================================

UIButton = {}

function UIButton.new(config)
    -- Stub: Create clickable button
    return {}
end

print("^2[FD-UI Button] Loaded (Stub)^0")
